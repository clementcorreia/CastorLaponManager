{% extends "::base.html.twig" %}

<div class="container">
    <div class="row center">
        <div class="col s6 offset-s3">
            <h3>{{ project.nom }}</h3>
        </div>
    </div>
    <div class="row">
        <div class="col s3">Equipe :</div>
        <div class="col s9">{{ equipe.nom }}</div>
    </div>
    <div class="row">
        <table class="responsive-table stripped centered">
            <thead>
            <tr>
                <th>Nom</th>
                <th>Prénom</th>
            </tr>
            </thead>
            <tbody>
            {% for user in equipe %}
                <tr>
                    <td>{{ user.nom }}</td>
                    <td>{{ user.prenom }}</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>

    <div class="row">
        <div class="col s12">
            {{ ticket.texte }}
        </div>
    </div>

    <!-- TODO -->
    <div class="row">
        <form class="col s12" action="{{ <X> }}" method="post">
            <div class="row">
                <div class="input-field col s12">
                    <textarea id="commentaireTicket" class="materialize-textarea"></textarea>
                    <label for="commentaireTicket">Textarea</label>
                </div>
            </div>
            <div class="row">
                <div class="col s6 offset-s3 center">
                    <button class="btn waves-effect waves-light light-green lighten-3" type="submit">Ok
                        <i class="material-icons right">arrow_forward</i>
                    </button>
                </div>
            </div>
        </form>
    </div>



    <!-- TODO Validation du ticket -->

</div>